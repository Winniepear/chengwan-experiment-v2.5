# V2.2安全与数据边界

生产入口为Netlify Functions + Netlify Database。没有公开默认管理员密码，也不使用本地测试数据库保存真实受试者数据。

## 必须做到

- 在Netlify环境变量中设置私有 `ADMIN_TOKEN`，建议至少32个不可预测字符，作用域包含Functions；不提交Git、不发给受试者。变更后重新部署。
- 后台通过 `X-Admin-Token` 请求数据。前端仅在当前标签会话的sessionStorage暂存令牌，清除按钮会删除；不能防御用户自己电脑上的恶意扩展或已注入脚本。
- 只用HTTPS生产域名收集；控制仓库、后台和数据库权限。公开站点不应公开 `config/`、`reference/`、`docs/`、`tests/`。
- 导出后做好访问控制和备份。不要删除或修改已应用的数据库迁移，不直接批量编辑受试记录。
- 填写真实研究联系方式和伦理信息；程序提示不等于获得伦理许可。

## 工程已做的保护

参数化SQL、每页白名单/数据范围/顺序检查、随机化事务锁、重复提交校验、元数据版本隔离、管理员令牌校验、CSV文本公式防护、HTTP no-store和基础安全头。不展示其他组评论或研究目标。

应用不主动写入姓名、电话、精确地址、IP或完整User-Agent；自动设备字段只存mobile/tablet/desktop/unknown。但托管平台可能存在其自身请求和运维日志，不能据此声称整个平台从未处理IP。请按研究要求核查平台日志策略、数据地域、跨境及保存期限。

匿名UUID用于浏览器恢复会话，属于会话访问凭据；知道有效UUID者可能访问对应会话。不要公开受试者ID或把它放进对外分享链接。本版没有独立的每人登录认证、实名去重、CAPTCHA或外部招募平台一次性访问码，不能识别同一真人更换设备/隐私窗口重复参加。

## 测试与已知边界

`tests/preview-server.mjs`仅监听127.0.0.1，内存SQLite和固定测试令牌只供本地合成测试；生产入口不导入它。测试数据不进入本交付包。

本轮未完成生产依赖安装和Netlify/PostgreSQL端到端验收，也未完成负载、渗透或DDoS防护测试。控制公开招募流量，监测函数错误和数据库用量；不要把基本输入校验当作完整安全审计。
