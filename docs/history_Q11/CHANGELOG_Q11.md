# V2.1 → V2.2：R10/Q10 → R11/Q11

## 严格按批准材料接入

29题；新增planning_1—3；将中文能力与社媒经验合并eligibility_basic；品牌基线移至共同帖子之后并与自身前测合页；删除10个旧诊断/开放/人口学字段；注意题明确数值6；保留自身匹配前后测、评论PAD与访问意向；没有旅游目的调节题。

城市简介、统一帖子、标签、昵称、24条评论及3张城市图片/7张头像不改写。材料号更新R11，刺激文本源版本记录R10，波次设PRETEST-W4。

## 工程接入与一致性

- 使用批准的JSON作为题干/端点/选项来源，构建时自动生成Functions配置和问卷快照；仅公开参与者所需部分，不公开其他组或研究目标。
- 新增planning页，合并baseline页，合并final_checks；去掉所有已删页的渲染与必答拦截。前后措辞匹配、不预选、后测不回显前次答案。
- 后端按步骤锁定数据：先同意、规划、城市、帖子、前测，才允许随机化。分页写入事务化；完全相同的网络重试可幂等成功，已经完成的旧页不允许覆盖。
- 规划均分、self配对变化、未施测记忆为NULL、未答注意为NULL，四类CSV继续输出。规范post与旧self别名同事务写入；旧记录不回填。
- 创建会话时保存问卷/UI/部署版本，而非导出时给历史数据套当前标签。
- 管理后台增加当前轮/全部历史导出选择；令牌只在sessionStorage暂存并可清除。真实令牌仍从Netlify环境变量读。
- 新问卷由CSS/JS版本参数更新缓存；移动端题目按钮不悬浮遮挡答案。
- 原三表和原SQL迁移逐字节保留，无删除/重建数据操作。

## 未包含

不新增统计结果；不更改预期PAD方向或清理规则；不额外缩短既定阅读门槛；不把墙钟时间升级声称为真正阅读时长；不提供生产管理员令牌、不部署用户账户、不保证假设显著。依赖仍待联网环境安装与生产构建验收。
