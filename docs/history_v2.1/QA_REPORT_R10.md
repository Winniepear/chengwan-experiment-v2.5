# R10 / V2.1 实际核验记录

日期：2026-09-08。仅报告本次实际运行的检查。

## 通过
- npm run build：配置同步、材料结构与24条文本、图片路径、6个共同事实、前后题目逐项一致、哈希生成。
- node --check：public/app.js、admin.js、生产API入口及api-core.mjs。
- 16项本地API测试：全部通过。涵盖前测完整性与范围、不能跳过帖子/前测、随机前不返回评论、随机后不能覆盖前测、规范后测及旧别名原子保存、变化值/标准化/缺失处理、旧版隔离、配额约束与幂等、认证和4类导出。
- 本地浏览器：320、390、768、1366宽度分别完成一组，覆盖G1—G4；前后页面文字相同、无预选、无前次答案；评论文本与配置相同，城市简介无图片标签，帖子有3图但无评论。未发现页面JavaScript异常或横向溢出。
- 四个浏览器会话的前测3/5/4、后测5/3/6、原字段后测别名和提交状态均通过后端读取核对。管理页及4类CSV请求成功。
- Word渲染14页，完整24条评论与配置原文比对，前测/后测与其他题目存在；逐页版面检查。
- Excel 7页，字符公式结果315/340/340/355，公式错误扫描0；预览已检查。
- 三张城市图、七张头像、城市/帖子/评论者配置、原SQL迁移与R9逐一比对不变。

## 测试环境与限制
本地核心API运行在SQLite方言转换适配器，pg_advisory_xact_lock调用只记录，不能证明真实PostgreSQL并发锁正确。生产入口仍为@netlify/database。

Chromium环境禁止原生网络导航，浏览器核验使用真实源HTML/CSS/JS离线渲染，fetch由Python桥接转到本地实际API，PNG以原字节data URI加载；不是Netlify实网浏览器测试。原25/25/45秒计时未快进。只在本地测试配置将每组配额设1以覆盖全部四组，发布配置仍为40。结束关闭测试页面时有桥接回调TargetClosedError警告，四个完整会话及后台导出断言均已完成，没有页面JS异常。无法以此替代iPhone、Android、微信实机验收。

本次npm依赖安装/锁文件解析尝试因registry.npmjs.org的EAI_AGAIN失败。没有声称npm install、SDK生产打包、Netlify发布和PostgreSQL连通通过；联网部署前需验证依赖并保留锁文件。

关键词强化、前测插入与分屏可能影响自然性、需求特征和总时长。尚未做人类认知访谈、操纵校准或正式效果检验，也未增加自动筛除规则。

详见QA_REPORT_R10.json及docs/qa/r10_api_test.log、r10_build.log。
