# R9 → R10 / V2.0 → V2.1 变更记录

## 用户指定范围
1. 增加自身PAD前测。
2. 保留匹配后测。
3. 评论情绪表达前置，并增加对应情绪关键词。

## 材料
G1的6条评论保持R9；G2/G3/G4的18条评论按上述范围重写。原城市、帖子、标签、3图、7头像、6昵称、顺序、互动数字和共同事实不变。前后测说明改成不依赖阶段的同一句；题干与端点仍取R9，不新增额外情绪量表。

## 页面与后端
为将前测放在共同帖子之后，新增post_intro和self_pad_pre；增加仅后端过渡状态randomize_ready，以支持前测已保存但随机请求丢失后的恢复。随机化从品牌前测后推迟到自身前测后。评论单独呈现，标题引用原帖子，但不重复大图正文。新帖子阅读门槛25秒需技术试运行。

规范后测字段self_P_post/A_post/D_post与原self_P/A/D后测别名同事务保存；前测字段self_P_pre/A_pre/D_pre为新增真实作答。导出逐维原值、标准化值、变化、标准化变化及配对有效标记。缺失前测保持空白，历史版本不改写。原三表及初始SQL迁移不变。

本次未把研究者目标或某组预期情绪显示给受试者，不改变品牌量表与三个主要对比、不新增自动删样本逻辑、不增加前三项之外的量表。不以软件测试代替材料校准。
