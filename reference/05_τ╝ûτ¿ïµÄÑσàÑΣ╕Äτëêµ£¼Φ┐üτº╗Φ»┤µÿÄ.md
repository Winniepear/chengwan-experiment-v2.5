# Q13／29项编程接入规范

本工程已经接入Q13。Q13不是删减版：questions数组严格29个变量，变量顺序、页面、必答规则与R11一致。

上线前：停止R11在途会话；备份历史CSV；部署R13后核对 `/api/health` 为 R13/Q13/PRETEST-W6；完整提交一份测试记录并检查宽表。旧R11记录不得重标为R13。

A/D仍使用R11字段名，目的是保持数据结构稳定；分析时必须联合 `revision_id` 判断题项版本。
